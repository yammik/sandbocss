import React from 'react';

const Navbar = () => {


  return (
    <div id="navbar">
      <div id="title" onClick={() => window.location.reload()}>
        sandboCSS
      </div>
    </div>
  )
}

export default Navbar;
